# Frontend code. 

The user likely is going to give you unspecific UI related code. All code in this repository is AI generated so it likely has issues associated with it. 

Always ask the user for every feature you want to add even if it is a small one (eg adding dark v light modes all the way to how you should structure your packages)

Always enter planning mode if the work required is non-trivial. Do not hesitate to push back and break it into smaller chunks

Use subagents liberally to achieve your goal so that your context length is always kept manageable. 

Always make sure you and your agents read the file ./normalDevelopment.md before starting to commit code. 

# Useful folders
## Normal development
./normalDevelopment.md