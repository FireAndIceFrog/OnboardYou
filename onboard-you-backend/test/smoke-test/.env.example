# ── OnboardYou Smoke Test Credentials ────────────────────────
# Copy to .env and fill in, or run `pnpm sync-env` to pull from tofu output.

API_BASE_URL=https://REPLACE_ME.execute-api.eu-west-1.amazonaws.com/v1
SMOKE_EMAIL=demo@onboardyou.com
SMOKE_PASSWORD=REPLACE_ME
