// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { CreateConfigData, CreateConfigErrors, CreateConfigResponses, GetConfigData, GetConfigErrors, GetConfigResponses, GetSettingsData, GetSettingsErrors, GetSettingsResponses, ListConfigsData, ListConfigsErrors, ListConfigsResponses, LoginData, LoginErrors, LoginResponses, UpdateConfigData, UpdateConfigErrors, UpdateConfigResponses, UpsertSettingsData, UpsertSettingsErrors, UpsertSettingsResponses, ValidateConfigData, ValidateConfigErrors, ValidateConfigResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * POST /auth/login
 *
 * Authenticate with email + password and receive Cognito tokens.
 * This endpoint does **not** require a JWT — it is the entry point
 * for obtaining one.
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>) => (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /config
 *
 * List all pipeline configurations owned by the caller's organization.
 * The organization is resolved from the caller's JWT claims.
 */
export const listConfigs = <ThrowOnError extends boolean = false>(options?: Options<ListConfigsData, ThrowOnError>) => (options?.client ?? client).get<ListConfigsResponses, ListConfigsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/config',
    ...options
});

/**
 * GET /config/{customerCompanyId}
 *
 * Retrieve the pipeline configuration for a given customer company.
 * The organization is resolved from the caller's JWT claims.
 */
export const getConfig = <ThrowOnError extends boolean = false>(options: Options<GetConfigData, ThrowOnError>) => (options.client ?? client).get<GetConfigResponses, GetConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/config/{customer_company_id}',
    ...options
});

/**
 * POST /config/{customerCompanyId}
 *
 * Create a new pipeline configuration. Validates the pipeline manifest,
 * persists to DynamoDB, and creates an EventBridge schedule.
 * The organization is resolved from the caller's JWT claims.
 */
export const createConfig = <ThrowOnError extends boolean = false>(options: Options<CreateConfigData, ThrowOnError>) => (options.client ?? client).post<CreateConfigResponses, CreateConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/config/{customer_company_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * PUT /config/{customerCompanyId}
 *
 * Update an existing pipeline configuration. Validates the pipeline manifest,
 * persists to DynamoDB, and updates the EventBridge schedule.
 * The organization is resolved from the caller's JWT claims.
 */
export const updateConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/config/{customer_company_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * POST /config/{customerCompanyId}/validate
 *
 * Dry-run column propagation: parses the pipeline manifest, builds every
 * action, and folds `calculate_columns` through the chain. Returns the
 * column set after each step without executing any real transformations.
 */
export const validateConfig = <ThrowOnError extends boolean = false>(options: Options<ValidateConfigData, ThrowOnError>) => (options.client ?? client).post<ValidateConfigResponses, ValidateConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/config/{customer_company_id}/validate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * GET /settings
 *
 * Retrieve the settings for the caller's organization.
 * The organization is resolved from the caller's JWT claims.
 */
export const getSettings = <ThrowOnError extends boolean = false>(options?: Options<GetSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetSettingsResponses, GetSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/settings',
    ...options
});

/**
 * PUT /settings
 *
 * Create or update the settings for the caller's organization.
 * The organization is resolved from the caller's JWT claims.
 */
export const upsertSettings = <ThrowOnError extends boolean = false>(options: Options<UpsertSettingsData, ThrowOnError>) => (options.client ?? client).put<UpsertSettingsResponses, UpsertSettingsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
