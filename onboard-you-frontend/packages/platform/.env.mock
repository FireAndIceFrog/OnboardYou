# ── Mock mode overrides ──────────────────────────────────────
# Loaded by:  pnpm dev:mock  (which runs vite --mode mock)

VITE_API_BASE_URL=http://localhost:3000
VITE_MOCK_MODE=true
VITE_CONFIG_REMOTE_URL=http://localhost:5174
